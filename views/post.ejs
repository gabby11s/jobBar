<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/post.css">
</head>
<body>
    <nav class="top-controls">
        <button onclick="location.href='/'">Home</button>
        <button onclick="location.href='/companies'">Companies</button>
    </nav>

    <div class="container">
      <div class="post-card">
        <h1>Post your Company</h1>
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="form-error" style="color:#ffd1d9;margin-bottom:0.75rem;font-weight:700;"><%= error %></div>
                <% } %>
        <form action="/post" method="POST">
            <div class="form-row">
                <label for="name">Company Name:</label>
                                <input type="text" id="name" name="name" required value="<%= form && form.name ? form.name : '' %>">
            </div>
            <div class="form-row">
                <label for="description">Description:</label>
                                <textarea id="description" name="description" required><%= form && form.description ? form.description : '' %></textarea>
            </div>
            <div class="form-row">
                <label for="link">Website URL:</label>
                                <input type="url" id="link" name="link" required value="<%= form && form.link ? form.link : '' %>">
            </div>
                                <input type="hidden" name="owner_id" value="<%= fb_id %>">
            <button type="submit">Submit</button>
        </form>
      </div>
    </div>
</body>
</html>

